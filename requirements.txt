# Core dependencies for Assignment 4: A2A Multi-Agent System

# Google Agent Development Kit
# Pinned: 2.x is required for the `McpToolset` class alias used by the tests
# (1.9.0 only ships `MCPToolset`). 2.4.0 is the last line that still pairs with
# a2a-sdk 0.3.x; 2.5.0 bumps to a2a-sdk 1.x, which drops TransportProtocol.
google-adk==2.4.0

# A2A SDK
# Pinned <1.0: create_agents.py imports TransportProtocol from a2a.types, which
# was removed in a2a-sdk 1.x.
a2a-sdk>=0.3.0,<1.0

# HTTP client for A2A communication
httpx>=0.27.0

# ASGI server for agent servers
uvicorn>=0.30.0

# Environment variable management
python-dotenv>=1.0.0

# Async event loop compatibility
nest-asyncio>=1.6.0

# MCP SDK (also in mcp_server/requirements.txt)
mcp>=1.6.0
