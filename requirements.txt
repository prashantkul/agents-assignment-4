# Core dependencies for Assignment 4: A2A Multi-Agent System

# Google Agent Development Kit
# Pinned: newer google-adk releases (2.x) require a2a-sdk 1.x, which renamed/moved
# APIs this starter kit depends on (TransportProtocol, ClientEvent, McpToolset).
# google-adk==1.9.0 also predates the McpToolset naming used here, so we pin to a
# later 1.x release that still resolves a2a-sdk<0.4 and exports McpToolset.
google-adk==1.36.1

# A2A SDK
a2a-sdk==0.3.26

# HTTP client for A2A communication
httpx>=0.27.0

# ASGI server for agent servers
uvicorn>=0.30.0

# Environment variable management
python-dotenv>=1.0.0

# Async event loop compatibility
nest-asyncio>=1.6.0

# MCP SDK (also in mcp_server/requirements.txt)
mcp>=1.6.0
